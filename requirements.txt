faster-whisper>=1.0.0
# 0.4.6+ required with numpy 2.x: older soundcard calls numpy.fromstring,
# removed in numpy 2.0 ("Capture error: The binary mode of fromstring...").
soundcard>=0.4.6
numpy>=1.24.0
# PyTorch bundles CUDA 12 DLLs (cublas64_12.dll etc.) on Windows.
# For GPU builds install torch FIRST from the CUDA index (setup.bat does this):
#   pip install "torch>=2.0.0" --index-url https://download.pytorch.org/whl/cu121
# then install the rest normally:
#   pip install -r requirements.txt
# (--index-url must NOT be combined with -r: it replaces PyPI entirely and the
# PyTorch index does not host the other packages below.)
torch>=2.0.0
# Current cuBLAS for newer GPUs (RTX 50xx): torch cu121's bundled copy is too
# old and fails with CUBLAS_STATUS_NOT_SUPPORTED; transcriber.py preloads this
# wheel's DLLs so they win over torch's. Harmless on CPU-only machines.
nvidia-cublas-cu12>=12.4
psutil>=5.9.0
pynvml>=11.0.0
# Offline English → Russian translation (no torch/transformers dependency).
# Download the language pack once by running:  python download_translation_model.py
argostranslate>=1.9.0
