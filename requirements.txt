faster-whisper>=1.0.0
soundcard>=0.4.3
numpy>=1.24.0,<2.0
# PyTorch bundles CUDA 12 DLLs (cublas64_12.dll etc.) on Windows.
# Install from the CUDA 12.1 index so the right binaries are fetched:
#   pip install -r requirements.txt --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
psutil>=5.9.0
pynvml>=11.0.0
# Required for the English → Russian translation tokenizer (OPUS-MT).
# The translation model weights are downloaded separately by running
# download_translation_model.py (one-time, ~80 MB).
transformers>=4.20.0
sentencepiece>=0.1.99
